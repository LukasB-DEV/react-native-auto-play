///
/// HybridHybridSearchTemplateSpec_cxx.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

import Foundation
import NitroModules

/**
 * A class implementation that bridges HybridHybridSearchTemplateSpec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
open class HybridHybridSearchTemplateSpec_cxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::at::g4rb4g3::autoplay::hybrid::bridge::swift`)
   * from `NitroAutoplay-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.at.g4rb4g3.autoplay.hybrid.bridge.swift

  /**
   * Holds an instance of the `HybridHybridSearchTemplateSpec` Swift protocol.
   */
  private var __implementation: any HybridHybridSearchTemplateSpec

  /**
   * Holds a weak pointer to the C++ class that wraps the Swift class.
   */
  private var __cxxPart: bridge.std__weak_ptr_HybridHybridSearchTemplateSpec_

  /**
   * Create a new `HybridHybridSearchTemplateSpec_cxx` that wraps the given `HybridHybridSearchTemplateSpec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: any HybridHybridSearchTemplateSpec) {
    self.__implementation = implementation
    self.__cxxPart = .init()
    /* no base class */
  }

  /**
   * Get the actual `HybridHybridSearchTemplateSpec` instance this class wraps.
   */
  @inline(__always)
  public func getHybridHybridSearchTemplateSpec() -> any HybridHybridSearchTemplateSpec {
    return __implementation
  }

  /**
   * Casts this instance to a retained unsafe raw pointer.
   * This acquires one additional strong reference on the object!
   */
  public func toUnsafe() -> UnsafeMutableRawPointer {
    return Unmanaged.passRetained(self).toOpaque()
  }

  /**
   * Casts an unsafe pointer to a `HybridHybridSearchTemplateSpec_cxx`.
   * The pointer has to be a retained opaque `Unmanaged<HybridHybridSearchTemplateSpec_cxx>`.
   * This removes one strong reference from the object!
   */
  public class func fromUnsafe(_ pointer: UnsafeMutableRawPointer) -> HybridHybridSearchTemplateSpec_cxx {
    return Unmanaged<HybridHybridSearchTemplateSpec_cxx>.fromOpaque(pointer).takeRetainedValue()
  }

  /**
   * Gets (or creates) the C++ part of this Hybrid Object.
   * The C++ part is a `std::shared_ptr<HybridHybridSearchTemplateSpec>`.
   */
  public func getCxxPart() -> bridge.std__shared_ptr_HybridHybridSearchTemplateSpec_ {
    let cachedCxxPart = self.__cxxPart.lock()
    if cachedCxxPart.__convertToBool() {
      return cachedCxxPart
    } else {
      let newCxxPart = bridge.create_std__shared_ptr_HybridHybridSearchTemplateSpec_(self.toUnsafe())
      __cxxPart = bridge.weakify_std__shared_ptr_HybridHybridSearchTemplateSpec_(newCxxPart)
      return newCxxPart
    }
  }

  

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  @inline(__always)
  public var memorySize: Int {
    return MemoryHelper.getSizeOf(self.__implementation) + self.__implementation.memorySize
  }

  /**
   * Call dispose() on the Swift class.
   * This _may_ be called manually from JS.
   */
  @inline(__always)
  public func dispose() {
    self.__implementation.dispose()
  }

  // Properties
  

  // Methods
  @inline(__always)
  public final func createSearchTemplate(config: SearchTemplateConfig) -> bridge.Result_void_ {
    do {
      try self.__implementation.createSearchTemplate(config: config)
      return bridge.create_Result_void_()
    } catch (let __error) {
      let __exceptionPtr = __error.toCpp()
      return bridge.create_Result_void_(__exceptionPtr)
    }
  }
  
  @inline(__always)
  public final func updateSearchResults(templateId: std.string, results: bridge.std__optional_NitroSection_) -> bridge.Result_void_ {
    do {
      try self.__implementation.updateSearchResults(templateId: String(templateId), results: results.value)
      return bridge.create_Result_void_()
    } catch (let __error) {
      let __exceptionPtr = __error.toCpp()
      return bridge.create_Result_void_(__exceptionPtr)
    }
  }
}
